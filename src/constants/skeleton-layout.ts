/** Skeleton counts aligned with real grid / rail layouts (see collection-ui + globals.css). */

/** COLLECTION_CARD_GRID: 1 → 2 → 3 → 4 columns */
export const SKELETON_COLLECTION_GRID_COUNT = 8;

/** Home discover / owned horizontal rail (becomes grid md+) */
export const SKELETON_HOME_RAIL_COUNT = 4;

/** Board detail item grid: 2 → 3 → 4 columns */
export const SKELETON_BOARD_ITEMS_COUNT = 8;

/** Profile boards tab */
export const SKELETON_PROFILE_GRID_COUNT = 4;

/** Explore list view rows */
export const SKELETON_EXPLORE_LIST_COUNT = 5;
