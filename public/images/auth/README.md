# Auth hero imagery

| File | Use |
|------|-----|
| `velvet-hero.png` | Single shared visual for all auth screens (`AuthHero`, `AUTH_HERO_IMAGE`) |

Crop is anchored **left** so the in-image quote (“Inspiration starts with a single idea.”) stays visible on desktop and mobile.

Regenerate from a new master only when replacing the brand auth visual — keep one file, one path.
